apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: audiobookshelf-upload-tls
  namespace: audiobookshelf
spec:
  secretName: audiobookshelf-upload-tls
  issuerRef:
    name: letsencrypt-prod-dns01
    kind: ClusterIssuer
  dnsNames:
    - abs-upload.dklair.io
