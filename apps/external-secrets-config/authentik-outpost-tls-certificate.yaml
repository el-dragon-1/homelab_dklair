apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-outpost-tls
  namespace: authentik
spec:
  secretName: authentik-outpost-tls
  issuerRef:
    name: letsencrypt-prod-dns01
    kind: ClusterIssuer
  commonName: "*.dklair.io"
  dnsNames:
    - "*.dklair.io"
