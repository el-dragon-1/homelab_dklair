# apps/external-secrets-config/pgadmin-admin-externalsecret.yaml
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: pgadmin-admin
  namespace: pgadmin
  annotations:
    argocd.argoproj.io/sync-wave: "1" # Ensure this syncs before the Helm app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: pgadmin-admin
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        email: "{{ .email }}"
        password: "{{ .password }}"
        secret_key: "{{ .secret_key }}"
  data:
    - secretKey: email
      remoteRef:
        key: homelab/pgadmin/pgadmin-admin
        property: email
    - secretKey: password
      remoteRef:
        key: homelab/pgadmin/pgadmin-admin
        property: password
    - secretKey: secret_key #rewquired to sync the secret_key for pgAdmin's sticky session cookie from Vault.
      remoteRef:
        key: homelab/pgadmin/pgadmin-admin
        property: secret_key