apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: openwrt-orchid-auth
  namespace: openwrt-ops
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: openwrt-orchid-auth
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: host
      remoteRef:
        key: homelab/openwrt/orchid
        property: host
    - secretKey: port
      remoteRef:
        key: homelab/openwrt/orchid
        property: port
    - secretKey: username
      remoteRef:
        key: homelab/openwrt/orchid
        property: username
    - secretKey: ssh_private_key
      remoteRef:
        key: homelab/openwrt/orchid
        property: ssh_private_key
    - secretKey: mesh_key
      remoteRef:
        key: homelab/openwrt/orchid
        property: mesh_key
