# CloudNativePG Operator values
replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

# Monitoring disabled for now, can enable later with Prometheus
monitoring:
  enabled: false